import Background from './Background';
import Colors from './Colors';
import FontSize from './FontSize';

export const allBackgrounds = Background;
export const allColors = Colors;
export const allFontSizes = FontSize;
